# Scenario Benchmarks

World, public adapter, and broad-phase scaling scenarios with mixed scenes.

Includes a pipeline breakdown benchmark that reports AABB update, broadphase,
and narrowphase timings for 1k and 10k object scenes.

Includes a public `DartCollisionDetector` adapter benchmark for collision,
dirty-world collision, distance, and raycast paths.
