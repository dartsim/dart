"""Per-scene factory modules for the dartpy consolidated demos runner.

Each module exposes a single ``build() -> SceneHandle`` function. See
``python/examples/demos/registry.py`` for the ``SceneHandle`` contract and the
frozen catalog (id/title/category/summary) each module is registered under.
"""
