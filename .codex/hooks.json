{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "^Bash$",
        "hooks": [
          {
            "type": "command",
            "command": "repo_root=\"$(git rev-parse --show-toplevel)\" && CLAUDE_PROJECT_DIR=\"$repo_root\" CODEX_PROJECT_DIR=\"$repo_root\" \"$repo_root/.claude/hooks/pre-commit-guard.sh\"",
            "commandWindows": "powershell -NoProfile -NonInteractive -ExecutionPolicy Bypass -Command \"& (Join-Path (git rev-parse --show-toplevel) '.claude/hooks/pre-commit-guard.ps1'); if (-not $?) { exit 2 }; if ($LASTEXITCODE -ne 0) { exit 2 }; exit 0\"",
            "timeout": 10,
            "statusMessage": "Checking DART commit gate"
          }
        ]
      }
    ]
  }
}
