# Note: The dart-gui component has been removed after consolidating on the OSG
# backend. OpenSceneGraph (OSG) is now the only GUI backend, provided through
# the dart-gui-osg component.

# Add subdirectories
add_subdirectory(osg)
