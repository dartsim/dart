# Resume: Local Review Gate

## Checkpoint (2026-09-06)

- Release worktree: `/home/js/dev/dartsim/dart/task_6_review_gate_release`,
  branch `fix/local-review-gate-6.20`, target `release-6.20`.
- Main worktree: `/home/js/dev/dartsim/dart/task_6_review_gate_main`, branch
  `fix/local-review-gate-main`, target `main`.
- Integrated bases at this checkpoint: release `e8f5b9a267f`; main `c5030dcab0fb`.
  Reverify current branch, HEAD, diff, fetched base and gate evidence before
  continuing. The Git-common review store owns immutable review records.
- The source original checkout was preserved. One writer implements; distinct
  read-only Astra Max sessions supply reviews with observed session settings.
- The shared Git hook is installed. Both branch doctors detect its exact
  checker; main also reports unobserved Codex trust for the fresh worktree,
  unrelated to Git enforcement.
- The user approved publication and CI/hosted-review cycles, including needed
  fixes, pushes and review requests. PRs #3492 and #3494 are already open.
  Merging, ready transitions, force-pushes and branch deletion are not included.
- Both first hosted rounds completed. Six confirmed finding families were
  independently recorded against release `3d7ecdb172` and main `c76439b35b`.
  The changing repair trees require fresh full local reviews; earlier clean
  reports are superseded by the adverse evidence. Do not duplicate the initial
  requests or mistake their completion for clean repaired-head coverage.
- Both complete local reviews of repair `adabe8dc3e` verified five hosted
  families fixed and kept custom-manager integrity open after reproducing an
  empty/truncated-runtime bypass. The subsequent installer export and its
  corruption/interruption tests need a new immutable candidate and full pair.

## Next Actions

1. Finish validation of the repaired trees. Initial native CPU/CUDA evidence
   and configured skips are recorded in `README.md`; affected AI checks and
   required full gates must cover the repaired candidates. Commit only intended
   files after full lint and reinstall the shared checker after runtime changes.
2. Prepare each intended outgoing commit. Obtain missing independent local
   correctness/contracts reports, repair complete finding batches, and repeat
   affected gates. If a current candidate already passes, do not add redundant
   review rounds. Earlier findings remain in the Git-common evidence history.
3. Refresh the existing PR bodies and branch-specific metadata. After merging
   the latest fetched base and passing the local publication gate, use the
   existing authorization to push additive repair commits. Do not open duplicate
   PRs. Preserve the release PR's externally selected ready state and main's
   draft state.
4. Finish native Windows CI and current-head hosted acceptance after approved
   publication. The initial release Windows gate step passed, but the repaired
   runtime needs new Windows execution. Follow trigger ownership and batching in
   `docs/onboarding/ai-reviews.md`.
5. Promote final durable conclusions and remove this active folder in the
   completing PR before final validation and reviews. Do not retire unfinished
   acceptance work without the required maintainer direction.

`README.md` owns scope and evidence. The blind replay and its remaining miss are
promoted to `docs/onboarding/ai-reviews.md`.

## Preserved Integration Backup

Main was fast-forwarded to `53c9495a95f` and its local changes reapplied from
stash `48885bfc1e35c17f5325db5c9f5116ac9b744672`. The PR-command conflict was
resolved by retaining upstream autonomy/writing guidance with the local gate.
The stash remains as a backup; do not confuse it with unrelated shared stashes.
The first main full run was stopped for integration. Only the resumed run on
the integrated base can establish final validation.
