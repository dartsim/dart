# Invalid evidence bundle

This bundle is retained only as superseded diagnostic history. It must not be
used for standing, timing, performance, or paper-parity claims.

The runner bound the protocol, runner, trace source, and trace executable, but
did not bind and recheck the dynamically resolved DART/shared-library build or
an independent collision-graph probe. Those provenance omissions were found
after the run. A fresh successor bundle is required; the existing result bytes
are intentionally preserved rather than retroactively upgraded.
