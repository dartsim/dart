# Superseded current-source binding

This provenance-complete scientific-negative bundle remains valid for the
source and executable identities recorded in its metadata, but it is no longer
the current-source bundle.

The shared trace later gained an opt-in, card-house-only manifold-sensitivity
schema and selector. The default and literal-arch schemas and experiment
contract were preserved, but the shared trace source/executable identities
changed. A fresh successor rerun therefore replaces this directory for
current-source claims.
