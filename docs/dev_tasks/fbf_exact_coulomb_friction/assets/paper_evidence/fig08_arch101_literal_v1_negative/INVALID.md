# Invalid artifact

This first authoritative attempt completed the frozen step-1 solve, but the
runner source changed during execution while its final adversarial hardening
landed. The runner's source-identity guard correctly classified the bundle as
`invalid_artifact`.

The raw prefix is retained only to prove that the drift guard worked. Do not
use it for the 101-stone convergence, standing, timing, or paper claims. Use
the later stable-source bundle named by the task ledger.
