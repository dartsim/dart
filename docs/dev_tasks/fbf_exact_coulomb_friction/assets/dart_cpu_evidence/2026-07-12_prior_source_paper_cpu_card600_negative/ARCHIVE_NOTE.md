# Prior-Source Card-House Negative Archive

This directory is a byte-identical repository archive of the bundle originally
written to `/tmp/fbf_cpu_paper_postreview_20260712_card600` at
`2026-07-12T05:13:24.041568+00:00`. The original metadata intentionally retains
that absolute output path. Archiving did not rerun or relabel the experiment.

The recorded trace executable SHA-256 is
`1688bcd13703dc9f58411bf2e83628f07ebd92fbf0e2eeb4b325748ef4182dfd`.
Later trace instrumentation changed the source and executable identities, so
this is explicitly prior-source historical evidence. It is not current-source
evidence and is not paper-comparable.

The artifact is a fail-closed scientific negative: 89 of 600 requested steps,
88 accepted capped steps, one exact failure, terminal residual
`0.018611716007362732`, and zero boxed-LCP fallbacks. Raw timing is diagnostic
only because the requested trajectory and solver contract fail.

`artifact-index.json` binds the sizes and SHA-256 values of the copied bundle
and this archive note. The index deliberately does not index itself.
