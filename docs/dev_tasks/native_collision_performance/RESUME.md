# Resume: Native Collision Performance

## Last Session Summary

Refreshed the benchmark manifest from current collision benchmark JSON after
additional primitive and public-adapter hot-path work. The manifest currently
has no `behind` rows, but it remains inventory evidence, not final acceptance
evidence, because CPU scaling was enabled and the run should be repeated under
quieter controlled conditions before completion.

## Current Branch

`task/native-collision-performance-complete` — active checkpoint branch for
this wave. Verify state with `git status --short --branch`; the latest intended
checkpoint contains primitive narrow-phase scalar contact insertion, public
DART adapter hot-path updates, refreshed manifest evidence, and task handoff
updates.

## Immediate Next Step

Audit the optimized contact manifold and public adapter pipeline changes for
cache-friendly data flow, backward compatibility, and gz-physics package
isolation before widening the refactor. Then repeat the broad collision
benchmark guard under controlled single-core conditions before promoting any
result as final acceptance evidence.

## Context That Would Be Lost

- The task docs intentionally do not name comparison-only implementations; use
  feature, algorithm, paper, or article language.
- The strongest-baseline rule applies per comparable benchmark family, not only
  to aggregate scores.
- Single-core CPU optimization comes before multi-core CPU or single-GPU API
  planning.
- Future parallel/accelerator work must prove wins against the optimized
  single-core path before public API or package commitments.
- For profiling-specific follow-up work, use profile builds and scoped
  `dart/common/profile.hpp` markers around candidate contact-manifold,
  public-adapter, and collision-pipeline stages instead of adding default-path
  instrumentation.
- `05-benchmark-manifest.md` is generated by
  `pixi run python scripts/generate_collision_benchmark_manifest.py --output docs/dev_tasks/native_collision_performance/05-benchmark-manifest.md`
  from `.benchmark_results/` JSON files.
- The current manifest records 45 leading comparable rows, 0 behind comparable
  rows, and 8 native-only rows.
- `pixi run -e collision-reference bm-collision-check-narrow-raw-reference`
  passed with `24 passed, 0 failed, 0 skipped` after the latest primitive
  hot-path changes.
- `pixi run -e collision-reference bm-collision-check-narrow-adapter` passed
  with `3 passed, 0 failed, 0 skipped` after the public adapter hot-path
  changes.
- Before task completion, remove `scripts/generate_collision_benchmark_manifest.py`
  with the temporary task evidence or retarget it to a durable dashboard output.

## How To Resume

```bash
git checkout task/native-collision-performance-complete
git status --short --branch
find docs/dev_tasks/native_collision_performance -maxdepth 1 -type f -print
```

Then continue from compatibility review and controlled benchmark rerun rather
than from a known manifest gap.
