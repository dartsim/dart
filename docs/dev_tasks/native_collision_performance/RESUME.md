# Resume: Native Collision Performance

## Last Session Summary

Created the multi-session planning folder for the post-feature-complete native
collision performance wave, then generated the first benchmark manifest from
the current collision benchmark JSON. A focused public-adapter edge-case slice
now leads in the refreshed noisy run after sync and box contact hot-path
improvements; the manifest remains inventory evidence, not final acceptance
evidence, because the host was noisy and CPU scaling was enabled.

## Current Branch

`task/native-collision-performance-exec` — branch is based on `origin/main`,
has the planning commit, and has local code, manifest, and script updates.

## Immediate Next Step

Investigate the largest remaining raw pair narrow-phase gap from
`05-benchmark-manifest.md`, implement the smallest safe hot-path improvement,
and regenerate targeted benchmark evidence.

## Context That Would Be Lost

- The task docs intentionally do not name comparison-only implementations; use
  feature, algorithm, paper, or article language.
- The strongest-baseline rule applies per comparable benchmark family, not only
  to aggregate scores.
- Single-core CPU optimization comes before multi-core CPU or single-GPU API
  planning.
- Future parallel/accelerator work must prove wins against the optimized
  single-core path before public API or package commitments.
- `05-benchmark-manifest.md` is generated by
  `scripts/generate_collision_benchmark_manifest.py` from `.benchmark_results/`
  JSON files.
- The current manifest records 22 leading comparable rows, 23 behind comparable
  rows, and 8 native-only rows.

## How To Resume

```bash
git checkout task/native-collision-performance-exec
git status --short --branch
find docs/dev_tasks/native_collision_performance -maxdepth 1 -type f -print
```

Then continue from the largest remaining raw pair narrow-phase gap.
