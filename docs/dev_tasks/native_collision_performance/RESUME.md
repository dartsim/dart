# Resume: Native Collision Performance

## Last Session Summary

Refreshed the benchmark manifest from current collision benchmark JSON after
additional primitive hot-path work. Raw capsule-sphere, capsule-box, and most
sphere-box edge rows now lead the strongest comparison timing; the manifest
still remains inventory evidence, not final acceptance evidence, because CPU
scaling was enabled and the task still has measured gaps.

## Current Branch

`task/native-collision-performance-exec` — active task branch for this wave.
Verify state with `git status --short --branch`; the latest intended checkpoint
contains the primitive hot-path changes, refreshed manifest, and resume updates
and should be pushed to `origin/task/native-collision-performance-exec` before
ending.

## Immediate Next Step

Continue with the remaining measured gaps from `05-benchmark-manifest.md`:
raw sphere-sphere, raw sphere-box deep penetration, and the two public adapter
edge rows. Verify any targeted fix with the focused benchmark row and the broad
collision guard.

## Context That Would Be Lost

- The task docs intentionally do not name comparison-only implementations; use
  feature, algorithm, paper, or article language.
- The strongest-baseline rule applies per comparable benchmark family, not only
  to aggregate scores.
- Single-core CPU optimization comes before multi-core CPU or single-GPU API
  planning.
- Future parallel/accelerator work must prove wins against the optimized
  single-core path before public API or package commitments.
- `05-benchmark-manifest.md` is generated by
  `pixi run python scripts/generate_collision_benchmark_manifest.py --output docs/dev_tasks/native_collision_performance/05-benchmark-manifest.md`
  from `.benchmark_results/` JSON files.
- The current manifest records 38 leading comparable rows, 7 behind comparable
  rows, and 8 native-only rows.
- `pixi run -e collision-reference bm-collision-check-narrow-raw-reference`
  passed with `19 passed, 0 failed, 0 skipped, 5 reported` after the latest
  primitive hot-path changes.
- Before task completion, remove `scripts/generate_collision_benchmark_manifest.py`
  with the temporary task evidence or retarget it to a durable dashboard output.

## How To Resume

```bash
git checkout task/native-collision-performance-exec
git status --short --branch
find docs/dev_tasks/native_collision_performance -maxdepth 1 -type f -print
```

Then continue from the largest remaining raw pair narrow-phase gap.
