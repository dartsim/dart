# Experimental Simulation Research References

The research-paper and reference catalog moved to
[`docs/readthedocs/papers.md`](../readthedocs/papers.md), which is now the
single source of truth and the published website page.

This file is kept only as a compatibility pointer for older links. Do not add
or update catalog entries here; update `docs/readthedocs/papers.md` through the
`dart-references` workflow instead.
