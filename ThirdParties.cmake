FUNCTION(LINK_THIRDPARTY_FREE_IMAGE project)
  IF (WIN32)
    TARGET_LINK_LIBRARIES(${project} debug libFreeImage_d optimized libFreeImage)
  ELSE()
    TARGET_LINK_LIBRARIES(${project} freeimage)
  ENDIF()
ENDFUNCTION(LINK_THIRDPARTY_FREE_IMAGE)

FUNCTION(LINK_THIRDPARTY_GFLAGS project)
  IF (WIN32)
    TARGET_LINK_LIBRARIES(${project} debug libgflags_d optimized libgflags)
  ELSE()
    TARGET_LINK_LIBRARIES(${project} gflags)
  ENDIF()
ENDFUNCTION(LINK_THIRDPARTY_GFLAGS)

FUNCTION(LINK_THIRDPARTY_GLOG project)
  IF (WIN32)
    TARGET_LINK_LIBRARIES(${project} debug libglog_d optimized libglog)
  ELSE()
    TARGET_LINK_LIBRARIES(${project} glog)
  ENDIF()
ENDFUNCTION(LINK_THIRDPARTY_GLOG)

FUNCTION(LINK_THIRDPARTY_GLUT project)
  TARGET_LINK_LIBRARIES(${project} ${OPENGL_LIBRARIES} ${GLUT_glut_LIBRARY})
ENDFUNCTION(LINK_THIRDPARTY_GLUT)

FUNCTION(LINK_THIRDPARTY_GTEST project)
  IF (WIN32)
    TARGET_LINK_LIBRARIES(${project} debug libgtest_d optimized libgtest)
  ELSE()
    TARGET_LINK_LIBRARIES(${project} gtest)
  ENDIF()
ENDFUNCTION(LINK_THIRDPARTY_GTEST)

FUNCTION(LINK_THIRDPARTY_GTEST_MAIN project)
  IF (WIN32)
    TARGET_LINK_LIBRARIES(${project} debug libgtest_main_d optimized libgtest_main)
  ELSE()
    TARGET_LINK_LIBRARIES(${project} gtest_main)
  ENDIF()
ENDFUNCTION(LINK_THIRDPARTY_GTEST_MAIN)

FUNCTION(LINK_THIRDPARTY_TICPP project)
  IF (WIN32)
    TARGET_LINK_LIBRARIES(${project} debug libticpp_d optimized libticpp)
  ELSE()
    TARGET_LINK_LIBRARIES(${project} ticpp)
  ENDIF()
ENDFUNCTION(LINK_THIRDPARTY_TICPP)
